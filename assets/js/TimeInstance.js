class TimeInstance
{
	constructor(day,startHour,startMin,endHour,endMin)
	{
		this.day = day;
		this.startHour = startHour;
		this.startMin = startMin;
		this.endHour = endHour;
		this.endMin = endMin;
	}
}